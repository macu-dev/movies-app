import styled from 'styled-components';
import renderCss from '@/helpers/renderCss';


const Column  = styled.div`
    ${renderCss}
    padding: 0 15px;
    position: relative;
    width: 100%;
`;

export default Column